import 'package:get/get.dart';
import 'gridphoto_item_model.dart';

class Iphone14NineModel {
  Rx<List<GridphotoItemModel>> gridphotoItemList =
      Rx(List.generate(18, (index) => GridphotoItemModel()));
}
